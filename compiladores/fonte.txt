iniciop
var linhas;
var iterador;
var j;
var coeficiente;
var pulaLinha;
pulaLinha = 0 - 9;

leia(linhas);

iterador = 0;

laco(iterador < linhas) faca
    j = 0;
    coeficiente = 1;

    laco(j <= iterador) faca
        escreva(coeficiente);
		
        coeficiente = coeficiente * (iterador - j) / (j + 1);

        j = j + 1;
    fimlaco;

    escreva(pulaLinha);

    iterador = iterador + 1;
fimlaco;
var wr;
fimp
