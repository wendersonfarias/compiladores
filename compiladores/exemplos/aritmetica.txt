iniciop
	var num;    
    var aux;   

    num = 1;
    aux = 2;

    leia(num);

    se(num > 5) entao
        aux = ((2 * 2) + (6 / 2)) + 12;
    senao
        escreva(0);
   	fimse;

   	escreva(aux);

fimp